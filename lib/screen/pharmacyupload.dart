import 'package:flutter/material.dart';

class PUploadPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Customer Prescription'),
      ),
      body: Center(
        // child: Text(
        //   'This is the P Page',
        //   style: TextStyle(fontSize: 24),
        // ),
      ),
    );
  }
}